diff --git a/build/shared.mk b/build/shared.mk
index 1e1afbd..4ba39c6 100644
--- a/build/shared.mk
+++ b/build/shared.mk
@@ -50,8 +50,8 @@ ifeq ($(CONFIG_DEBUG),y)
 FLAGS += -DDEBUG -rdynamic -O0 -ggdb -mssse3 -muintr
 LDFLAGS += -rdynamic
 else
-# FLAGS += -DNDEBUG -O3 -mssse3
-FLAGS += -DNDEBUG -O3 -mavx512f
+FLAGS += -DNDEBUG -O3 -mssse3
+# FLAGS += -DNDEBUG -O3 -mavx512f
 # FLAGS += -DNDEBUG -O3 -mavx2
 ifeq ($(CONFIG_OPTIMIZE),y)
 FLAGS += -march=native -flto -ffast-math
diff --git a/iokernel/dpdk.c b/iokernel/dpdk.c
index cf7d46d..2c19567 100644
--- a/iokernel/dpdk.c
+++ b/iokernel/dpdk.c
@@ -280,7 +280,7 @@ int dpdk_init(void)
 int dpdk_late_init(void)
 {
 	/* initialize port */
-	dp.port = 0;
+	dp.port = 1;
 	if (dpdk_port_init(dp.port, dp.rx_mbuf_pool) != 0) {
 		log_err("dpdk: cannot init port %"PRIu8 "\n", dp.port);
 		return -1;
diff --git a/iokernel/hw_timestamp.c b/iokernel/hw_timestamp.c
index c948d80..fd2bf97 100644
--- a/iokernel/hw_timestamp.c
+++ b/iokernel/hw_timestamp.c
@@ -104,7 +104,7 @@ int hw_timestamp_init(void)
 	}
 
 	for (i = 0; dev_list[i]; i++) {
-		if (strncmp(ibv_get_device_name(dev_list[i]), "mlx5", 4))
+		if (strncmp(ibv_get_device_name(dev_list[i]), "mlx5_3", 6))
 			continue;
 
 		if (!nic_pci_addr_str)
diff --git a/runtime/net/directpath/mlx5/mlx5_init_verbs.c b/runtime/net/directpath/mlx5/mlx5_init_verbs.c
index 537e715..29a93d7 100644
--- a/runtime/net/directpath/mlx5/mlx5_init_verbs.c
+++ b/runtime/net/directpath/mlx5/mlx5_init_verbs.c
@@ -469,7 +469,7 @@ int mlx5_verbs_init_context(bool uses_qsteering)
 	}
 
 	for (i = 0; dev_list[i]; i++) {
-		if (strncmp(ibv_get_device_name(dev_list[i]), "mlx5", 4))
+		if (strncmp(ibv_get_device_name(dev_list[i]), "mlx5_3", 6))
 			continue;
 
 		if (ibv_device_to_pci_addr(dev_list[i], &pci_addr)) {
